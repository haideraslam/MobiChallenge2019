For all 4 projects, go build the docker images. (Details in readme of each project)

The loc-tracking websocket server runs at 3005 port.

##Run all containers

`docker-compose up`

##Bring down all containers

`docker-compose down`